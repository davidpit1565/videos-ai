# Get a key pair at https://cloud.higgsfield.ai/api-keys
# Copy this file to .env (same folder) and fill in the real values.
# .env is gitignored — never commit real credentials.
HF_API_KEY=
HF_SECRET=
